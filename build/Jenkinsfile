pipeline {
    agent any 
    stages {
        stage('create cluster') {
            steps {
                withAWS(credentials: 'aws-creds', region: 'eu-central-1') {
                    sh 'aws s3 ls'
                }
            }
        }
    }
}