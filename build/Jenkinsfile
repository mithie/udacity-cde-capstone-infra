pipeline {
    agent any 
    stages {
        stage('create cluster') {
            steps {
                withAWS(roleAccount:'173411263722', role:'udacity-eks-admin') {
                    sh 'aws s3 ls'
                }
            }
        }
    }
}